import 'package:flutter/services.dart';
import 'package:flutter_test/flutter_test.dart';
//import 'package:flutter_coast_audio_miniaudio/flutter_coast_audio_miniaudio_method_channel.dart';

/*void main() {
  TestWidgetsFlutterBinding.ensureInitialized();

  MethodChannelFlutterCoastAudioMiniaudio platform = MethodChannelFlutterCoastAudioMiniaudio();
  const MethodChannel channel = MethodChannel('flutter_coast_audio_miniaudio');

  setUp(() {
    TestDefaultBinaryMessengerBinding.instance.defaultBinaryMessenger.setMockMethodCallHandler(
      channel,
      (MethodCall methodCall) async {
        return '42';
      },
    );
  });

  tearDown(() {
    TestDefaultBinaryMessengerBinding.instance.defaultBinaryMessenger.setMockMethodCallHandler(channel, null);
  });

  test('getPlatformVersion', () async {
    expect(await platform.getPlatformVersion(), '42');
  });
}*/
